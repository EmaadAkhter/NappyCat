{
  "_comment": "Emulator/dev config: short-duration rules so the app's dev timings are accepted. Launch with --project demo-tidal; the emulator serves whatever project the client asks for, and tests derive the id from firebase_options.",
  "firestore": {
    "rules": "firestore.dev.rules",
    "indexes": "firestore.indexes.json"
  },
  "emulators": {
    "firestore": {
      "port": 8089
    },
    "auth": {
      "port": 9099
    },
    "ui": {
      "enabled": true,
      "port": 4000
    },
    "singleProjectMode": true
  }
}